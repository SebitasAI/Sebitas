// Tailwind v4 PostCSS plugin. CSS-first config; no tailwind.config.js needed
// for the placeholder marketing site.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
