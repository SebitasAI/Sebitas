"""Generic credentialed-integrations layer via Pipedream Connect. Integrations
are data (the workspace connects an app); the agent invokes actions through a
gateway that injects auth server-side. Credentials never reach the model or us."""
